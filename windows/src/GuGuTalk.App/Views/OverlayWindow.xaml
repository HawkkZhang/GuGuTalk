<Window x:Class="GuGuTalk.App.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:GuGuTalk.App.Views.Controls"
        Title="GuGuTalk Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="320" Height="140"
        WindowStartupLocation="Manual">

    <Border CornerRadius="12"
            Background="{StaticResource SurfacePanelBrush}"
            BorderBrush="{StaticResource BorderSubtleBrush}"
            BorderThickness="1"
            Padding="16"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" Opacity="0.12" ShadowDepth="4" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title + Recording indicator -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <Ellipse Width="8" Height="8" Margin="0,0,8,0"
                         x:Name="RecordingDot"
                         Fill="{StaticResource IconAquaBrush}" />
                <TextBlock x:Name="TitleText"
                           Text="正在聆听"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" />
            </StackPanel>

            <!-- Waveform -->
            <controls:WaveformMeter Grid.Row="1"
                                    x:Name="Waveform"
                                    Height="20"
                                    BarBrush="{StaticResource IconAquaBrush}"
                                    Margin="0,0,0,8" />

            <!-- Transcript -->
            <TextBlock Grid.Row="2"
                       x:Name="TranscriptText"
                       TextWrapping="Wrap"
                       FontSize="14"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       MaxHeight="50"
                       TextTrimming="CharacterEllipsis" />

            <!-- Status message -->
            <TextBlock Grid.Row="3"
                       x:Name="StatusText"
                       FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0" />
        </Grid>
    </Border>
</Window>
