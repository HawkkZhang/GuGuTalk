# Project Codex Config

This directory stores project-local Codex context for `DesktopVoiceInput`.

Suggested usage:

- put project memory in `memory.md`
- add project-specific instructions here later if needed
- keep repo-local notes separate from the global `~/.codex` directory

Global Codex assets still live in:

- `/Users/end/.codex`

This folder is only for this repository.
